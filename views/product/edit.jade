extends ../layout

block content
    .container
        h1 Product Edit

        form(role='form', action="/edit", method="post", enctype="multipart/form-data", style='max-width: 300px;')
            include create-forms

            button(type="submit", value="Upload") Upload File